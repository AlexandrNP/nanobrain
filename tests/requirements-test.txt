# Test Dependencies for NanoBrain Framework
# These are required for running the test suite in CI/CD environments

# Core testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.1.0
pytest-cov>=4.0.0

# Async support
asyncio-mqtt>=0.16.0

# Performance testing
pytest-benchmark>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution

# Reporting and output
pytest-html>=4.0.0
pytest-json-report>=1.5.0

# Development and debugging
pytest-mock>=3.10.0
pytest-sugar>=0.9.7  # Better test output

# Framework dependencies (if not already installed)
pydantic>=2.0.0
pyyaml>=6.0
aiofiles>=23.0.0
asyncio-pool>=0.7.0

# Optional: For real component testing
# fastapi>=0.100.0
# websockets>=11.0.0
# pandas>=2.0.0 