name: "Sub-Workflow Configuration"
description: "Configuration for SubWorkflow step"
version: "1.0.0"

# Simple configuration for sub-workflow step
processing_delay: 1.0
workflow_prefix: "SubWorkflow processed"